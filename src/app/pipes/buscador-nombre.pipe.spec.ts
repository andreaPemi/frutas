import { BuscadorNombrePipe } from './buscador-nombre.pipe';

describe('BuscadorNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
